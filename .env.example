# Environment Variables
# Copy this to .env and fill in your values

# Authentication
PWORD=changeme
JWT_SECRET=changeme
NODE_ENV=development

# Vercel KV (automatically configured when you link KV in Vercel)
# These are auto-populated by Vercel - leave empty for local dev
# KV_URL=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
